[phases.setup]
nixPkgs = ['nodejs_21', 'nodejs-21_x']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'

[variables]
NODE_OPTIONS='--max-old-space-size=2048'
